#ifndef BROWSER_ENGINE_H\n#define BROWSER_ENGINE_H\n\n// Include necessary headers\n#include <string>\n#include <vector>\n#include <cef_app.h>\n#include <cef_client.h>\n#include <cef_render_handler.h>\n#include <cef_base.h>\n\nclass BrowserEngine : public CefApp, public CefBrowserProcessHandler, public CefLifeSpanHandler {\npublic:\n    BrowserEngine();\n    ~BrowserEngine();\n\n    // Initializes the browser engine\n    void Initialize();\n\n    // Create a new browser tab\n    void CreateTab(const std::string& url);\n    \n    // Download manager to handle file downloads\n    void DownloadFile(const std::string& url);\n\n    // Implementations of CefApp and CefBrowserProcessHandler methods\n    virtual CefRefPtr<CefBrowserProcessHandler> GetBrowserProcessHandler() OVERRIDE;\n    virtual void OnBeforeChildProcessLaunch(const CefString& command_line) OVERRIDE;\n\n    // Implementations of CefLifeSpanHandler methods\n    virtual void OnBeforePopup(CefRefPtr<CefBrowser> browser, const CefString& target_url, const CefString& target_frame_name, const CefString& popup_features, bool is_new_resizable) OVERRIDE;\n    virtual void OnAfterCreated(CefRefPtr<CefBrowser> browser) OVERRIDE;\n\nprivate:\n    std::vector<CefRefPtr<CefBrowser>> m_browsers; // Holds all browser instances\n\n    IMPLEMENT_REFCOUNTING(BrowserEngine);\n};\n\n#endif // BROWSER_ENGINE_H